/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  images: {
    domains: ["assets.myntassets.com"], // Add your image domains here
  },
};

export default nextConfig;
